import React      from 'react';
import IndexPage  from './pages/IndexPage';

import './index.css';

export default React.createClass({

  render() {
    return (
      <div>
        <IndexPage />
      </div>
    );
  }
});
